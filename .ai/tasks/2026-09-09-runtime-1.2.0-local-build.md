# RUNTIME-LOCAL-BUILD-20260909: Dừng batch sau video 07 và build Windows local

- **Trạng thái:** Đã kiểm chứng cục bộ; batch đã dừng theo yêu cầu và installer Windows đã đóng gói thành công.
- **Người thực hiện:** Codex
- **Thời gian:** 2026-09-09

## 1. Phạm vi

- Dừng job AutoShort sau khi video 07 kết thúc lỗi, không tiếp tục xử lý các video sau đó.
- Giữ các thay đổi đã thực hiện trên nhánh `codex/measured-dubbing-first`, fast-forward `main` vào cùng commit local.
- Xác minh runtime OCR 1.2.0, build ứng dụng và đóng gói installer Windows x64.

## 2. Kết quả batch

- Job: `63b6a6c4-5eba-48f5-b44c-dc951d199d6f`.
- Log: `F:/Test_video/autoflow-batch-live-20260909-rerun2.log`.
- Hoàn tất: video 01–06 (`6/15`).
- Video 07 dừng tại cue `cue-52-97820` vì cần kéo dài đoạn hình `341.4%`, vượt giới hạn `40%`; hệ thống không cắt lời.
- Tiến trình đã được dừng theo cây process; video 08 chỉ mới phát sinh dòng bắt đầu xử lý và không được hoàn tất.
- Không kết luận batch 15 video đạt nghiệm thu; video 07 vẫn là lỗi cần xử lý ở lượt sau.

## 3. Merge local

- Commit tích hợp: `f20ddc6 feat(autoshort): integrate OCR and measured dubbing fixes`.
- `main` local đã fast-forward tới `f20ddc6`.
- Không push remote và không xóa các file untracked có sẵn trong workspace.

## 4. Kiểm chứng runtime và build

Runtime user-data:

- `ocr-engine --version`: `1.2.0`, protocol `ocr-local/1`.
- `ocr-engine --probe`: `ready=true`, GPU `false` nên hiện dùng CPU fallback.
- Implementation fingerprint: `1e0c8bd778d9d97cf129c891e053b0421f5b43b4c3d0914e29e38c63b614c6a4`.
- Executable SHA-256 local: `7ac0d6fe1bc497544a60a74cd6ae66878158a8e772ea86c12ce3b1a3e190f781`.

Các lệnh chính:

```text
cmd.exe /d /c "npm.cmd run typecheck"              PASS
cmd.exe /d /c "npm.cmd run build"                  PASS
cmd.exe /d /c "npm.cmd run package:win"             PASS
```

Packaging verifier và font verification đều PASS. Bộ `npm run test:local-runtime` đã chạy hết nhưng exit `1` do các assertion còn lệch trong nhóm `dubbing-plan` và `translation-rephrase` (14 lỗi); đây là cảnh báo kiểm thử cần xử lý tiếp, không làm mất artifact đã đóng gói.

Installer local:

- [TediaPros-0.1.23-setup.exe](F:/Son/tool/TediaPros/dist/TediaPros-0.1.23-setup.exe) — 544,902,919 bytes.
- Blockmap: `F:/Son/tool/TediaPros/dist/TediaPros-0.1.23-setup.exe.blockmap`.
- Installer chưa ký chứng thư Windows; electron-builder đã ghi nhận signing bị bỏ qua do môi trường local không có certificate.

## 5. Việc cần làm tiếp

- Điều tra 14 assertion lỗi ở `dubbing-plan`/`translation-rephrase` và cập nhật contract/test theo hành vi measured-first và trần tempo `1.80x` hiện hành.
- Xử lý cue `cue-52-97820` của video 07 (reflow/split semantic group hoặc sửa bản dịch để giảm nhu cầu kéo dài).
- Chỉ chạy tiếp từ video 08 sau khi người dùng yêu cầu; đánh giá lại toàn bộ batch bằng real-media sau khi sửa.
