/**
 * User-facing branding for the desktop app.
 *
 * The legacy protocol/storage namespaces intentionally remain `tblao` for
 * backward compatibility; these constants are only for presentation and
 * release metadata.
 */
export const APP_BRAND = Object.freeze({
  displayName: 'TediaPros',
  legacyDisplayName: 'T-blao',
  packageName: 'tedia-pros'
})
